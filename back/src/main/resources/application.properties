spring.application.name=back
server.port = 8080

spring.datasource.url=${DB_END_POINT}
spring.datasource.username=${DB_USER_NAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
@@ -12,10 +12,9 @@ spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false


#naver
oauth.naver.client-id=${NAVER_CLIENT_ID}
oauth.naver.client-secret=${NAVER_CLIENT_SECRET}
oauth.naver.redirect-uri=${NAVER_REDIRECT_URL}


jwt.secret=${JWT_SECRET} 