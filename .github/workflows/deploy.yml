name: Docker-Based CI/CD for Spring Boot

on:
  push:
    branches: [ "main" ] # main 브랜치에 푸시될 때마다 워크플로우 실행

# 환경 변수 정의
env:
  DOCKER_IMAGE_NAME: back-app
  IMAGE_TAG: latest

jobs:
  # 1. 빌드 및 Docker Hub에 이미지 푸시 (CI 단계)
  build_and_push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      # 1.1. CI: Spring Boot JAR 파일 빌드
      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Build Spring Boot JAR (back/로 이동 및 빌드)
        working-directory: ./back
        run: |
          chmod +x gradlew
          ./gradlew clean build -x test

      # 💡 [핵심 수정 부분] 쉘 변수를 사용하여 JAR 파일을 안전하게 이동시킵니다.
      - name: Locate and Move JAR file safely
        run: |
          # 1. build/libs/ 폴더 내에 있는 첫 번째 JAR 파일을 변수에 할당합니다.
          JAR_FILE=$(ls -1 ./back/build/libs/*.jar | head -n 1)
          
          echo "Discovered JAR file: $JAR_FILE"
          
          if [ -z "$JAR_FILE" ]; then
              echo "Error: Could not locate any JAR file in ./back/build/libs/"
              exit 1
          fi
          
          # 2. 찾은 파일을 Dockerfile이 기대하는 이름으로 ./back/ 폴더로 이동
          #    (back/ 폴더가 Docker 빌드 컨텍스트의 루트입니다.)
          mv "$JAR_FILE" ./back/back-0.0.1-SNAPSHOT.jar
          
          echo "Move complete. Listing contents of ./back/ for verification:"
          ls -l ./back/ # 이동 확인용 디버깅 명령

      # 1.2. Docker: 로그인 및 이미지 빌드/푸시
      - name: Docker Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Build and Push Docker Image to Docker Hub
        uses: docker/build-push-action@v5
        with:
          # Dockerfile이 있는 ./back 폴더를 빌드 컨텍스트로 지정
          context: ./back 
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/${{ env.DOCKER_IMAGE_NAME }}:${{ env.IMAGE_TAG }}
          file: ./back/Dockerfile

  # 2. EC2에 접속하여 최신 Docker 이미지로 컨테이너 교체 (CD 단계)
  deploy:
    needs: build_and_push # 1번 Job이 성공해야만 실행
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to EC2 (Docker 컨테이너 교체)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          script: |
            DOCKER_USER=${{ secrets.DOCKER_HUB_USERNAME }}
            IMAGE_NAME=${DOCKER_USER}/${{ env.DOCKER_IMAGE_NAME }}:${{ env.IMAGE_TAG }}
            CONTAINER_NAME=runpt-back

            echo "Login to Docker Hub..."
            docker login -u ${DOCKER_USER} -p ${{ secrets.DOCKER_HUB_TOKEN }}
            
            echo "Pulling latest image: ${IMAGE_NAME}"
            docker pull ${IMAGE_NAME}
            
            # 기존 컨테이너 중지 및 삭제
            if [ $(docker ps -a -q -f name=${CONTAINER_NAME}) ]; then
              echo "Stopping and removing existing container..."
              docker stop ${CONTAINER_NAME}
              docker rm ${CONTAINER_NAME}
            fi
            
            # 새 이미지로 컨테이너 실행 (DB 환경 변수 안전하게 주입)
            echo "Running new ${CONTAINER_NAME} container with DB Secrets..."
            # -e 옵션을 사용하여 GitHub Secrets에 저장된 DB 정보를 전달
            docker run -d --name ${CONTAINER_NAME} \
              -p 127.0.0.1:8080:8080 \
              -e DB_END_POINT="${{ secrets.DB_END_POINT }}" \
              -e DB_USER="${{ secrets.DB_USER }}" \
              -e DB_PASSWORD="${{ secrets.DB_PASSWORD }}" \
              --restart unless-stopped \
              ${IMAGE_NAME}
            
            echo "🎉 Deployment completed!"